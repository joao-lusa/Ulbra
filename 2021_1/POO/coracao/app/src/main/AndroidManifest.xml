<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saude">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Saude">
        <activity android:name=".FinishActivity"></activity>
        <activity android:name=".IdadeActivity" />
        <activity android:name=".ColesterolActivity" />
        <activity android:name=".FamiliaActivity" />
        <activity android:name=".PressaoActivity" />
        <activity android:name=".FumoActivity" />
        <activity android:name=".AtividadeActivity" />
        <activity android:name=".PesoActivity" />
        <activity android:name=".SexoActivity" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>